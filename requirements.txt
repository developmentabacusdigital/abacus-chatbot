# Vercel's Python builder looks for requirements.txt at the repo root. The actual
# dependency list lives in backend/requirements.txt (used directly for local dev via
# `cd backend && pip install -r requirements.txt`) — this just points at it so the two
# never drift out of sync.
-r backend/requirements.txt
